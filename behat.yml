default:
    extensions:
        Behat\MinkExtension\Extension:
            base_url:  'http://localhost:8000/'
            goutte:     ~
            #selenium2: ~
            files_path: .

    context:
        parameters:
            base_url:  'http://localhost:8000/'

    filters:
        tags: "~@skipped&&~@api&&@ryl"

api:
    paths:
        features:  features/api
        bootstrap: %behat.paths.features%/bootstrap
    filters:
        tags: "~@skipped&&@api"
wip:
    filters:
        tags: "@skipped"