<header id=header class="home-parallax home-fade dark-bg header-inner inline-bg-img {{ settings.class }}">
    <div class=color-overlay></div>
    <div class=container>
    {% for child in block.children %}
        {% block block_child_render %}
            {{ sonata_block_render(child) }}
        {% endblock %}
    {% endfor %}
    </div>
</header>